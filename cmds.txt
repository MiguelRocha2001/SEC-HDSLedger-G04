Using maven:
    $ cd Service
    $ mvn compile exec:java -Dexec.args="1 regular_config.json"

Generate keys:
    $ openssl genrsa -out server1.key
    $ openssl rsa -in server1.key –pubout > public1.key